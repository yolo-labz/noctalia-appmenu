# Spec 006 close-out (Lane B)

**Status:** closed
**Closed:** 2026-05-20
**Shipped via:** PR #74

## Disposition

Spec 006 was Lane B of the v1.0.0 four-lane split (umbrella spec 004).
Scope: plugin QML completion — nested submenus (FR-010),
`toggle_state` rendering (FR-011), `icon_name` rendering (FR-012),
screen guard (FR-013).

All FRs shipped in PR #74 (nested submenus + toggle_state + icon_name
+ screen guard).

Subsequent plugin work (popup dismiss, self-heal cascade, visual
parity) shipped through specs 009/013/014/015.

## Successor specs

- **009** — Popup hotfix (v1.0.1) — closed.
- **014** — Popup dismiss redesign — PR #99 awaiting Pedro pick.
- **015** — Ship-ready completion (visual parity, self-heal) —
  mechanically done.

## Why this doc exists

Speckit-pipeline audit consistency. No code change. No follow-up tasks.
