# R5 — Noctalia quality and real demo

Evidence updated: 21/09/2026 10:24 BRT. Scoped implementation, not a completion or merge claim.

## R5b continuation (supersedes the earlier media outcome below)

OpenAI GPT-6 Astra/xhigh; no reviewer spawned, gate or merge claimed. Only branch
225-quality-media-swarm changed; 226 and sibling branches untouched.

Confirmed private activation root cause: default AT-SPI broker/systemd path
cannot activate Registry in this nested session. The identical probe goes from
NameHasNoOwner to successful Registry activation with
`ATSPI_DBUS_IMPLEMENTATION=dbus-daemon`; store service paths and details are in
[isolated-demo.md](how-to/isolated-demo.md). No live D-Bus configuration changed.

Actual captured evidence: [MP4](assets/isolated-legacy-demo/demo.mp4),
[GIF](assets/isolated-legacy-demo/demo.gif), [poster](assets/isolated-legacy-demo/poster.png).
MP4: 1280×720, 10 fps, 16 seconds, silent, 81424 bytes; GIF 768×432, 5 fps.
Two native menu snapshots assert AT-SPI source, app PID and populated children.
Bridge `atspi-click` opened a real About Okular dialog, asserted by niri window
inspection and observed pixels. This is **not a popup-row click claim**:
Help topbar click logged but no dropdown became visible. Full mouse-driven demo
remains unfulfilled; no current Rust-host support claim. Legacy QML v4.7.8-git,
bridge 1.0.36, actual plugin, Okular 26.08.0; provenance/recreation/cleanup and
upstream artwork attribution are in the recipe. No fake menu or private profile.

Verification: `r5b-baseline.txt` (red), `r5b-daemon.txt` (Registry green),
`r5b-media-check.txt` (both native menus and real action), per-app JSON receipts,
ffprobe and SHA256SUMS. Representative frames visually inspected; sampled OCR
retained, not an exhaustive frame audit. The helper refuses an expired Xvfb PID
(observed FileNotFoundError) rather than falling back to the daily display.
No bridge/plugin runtime code, checks, coverage, workflows or deployments changed.
Final preparation replay passed (`r5b-replay-check.txt` and durable replay
frames); Python compilation and `git diff --check` passed. Owned final probe
exited. At the worker deadline R5b changes were uncommitted (PR225 was ba16b777).
Coordinator subsequently inspected the actual poster, passed all ten asset hashes
from the repository root, checked Sonar roots and scanned docs with Gitleaks.
This follow-up commit preserves the partial capture; full dropdown behavior is
still unfulfilled. The coordinator delivery ledger records publication state.
PR225 remains OPEN; coordinator owns review/merge. Revert via one revert PR
if this commit is later landed. Earlier R5 phase report follows for history.


## Scope and provenance

- Repo: yolo-labz/noctalia-appmenu; exclusive branch `225-quality-media-swarm`.
- Base: `29d099559773cd78f79133bcbca8c5ec96b2eef3` (origin/main).
- Primary implementation head: `e082a749cc6965074bfef2a5ba50c92478566bf6`
  (subsequent commit only finalizes this report/tasks/check receipt).
- Generator: OpenAI GPT-6 Astra/xhigh, following
  pre-edit GLM-5.3 admission failure reported by coordinator.
- Coordinator supplies exact-head GLM-5.3 review and serializes merges. No worker
  merge/deploy, no independent verdict claimed.
- Spec: [017-quality-media](../specs/017-quality-media/spec.md).

## Hypothesis and observed causes

**Hypothesis:** the current Sonar failures are caused by a nonexistent configured
root, not server availability or tokens. Confirmed by jobs `106292512541` and
`106294087026`: authenticated scanner reaches Community Build 26.3, then reports
`Invalid value of sonar.tests` / `The folder 'tests' does not exist`.

| PR | Exact head | Evidence / disposition |
|---|---|---|
| [219](https://github.com/yolo-labz/noctalia-appmenu/pull/219) | `d4890b288b5d270c5446bbe078c3ce214c0bbcbf` | Latest bridge tests green (`106292551055`). Old coverage job `95353363896`: no space left on device. Old Sonar `95353363636`: runner 2G free below 5G floor. Bridge failure `106131529859` logs 404; job metadata shows tests passed, coverage interrupted. Do not invent a test assertion failure. |
| [222](https://github.com/yolo-labz/noctalia-appmenu/pull/222) | `03cafd8bb7ae1ad4b0e600f501cfd598893720ef` | Old title job `106131082372` replays original 73-character event title despite current 40-character title. New-title run `35530609123` title/tests green but Nix cancelled. Sonar and reproducibility green on original run. |
| [223](https://github.com/yolo-labz/noctalia-appmenu/pull/223) | `febc5e95b8177b1b70ea2e1b9cb7ff9e8ea6c451` | Sonar missing root. Reproducibility `106292514146` first build/tests pass, then unrelated devShell `cargo clean` fails because cargo-llvm-cov derivation is not valid; second build and comparison never execute. Not evidence of differing binary bytes. |
| [224](https://github.com/yolo-labz/noctalia-appmenu/pull/224) | `598b9cce978c8c0c3322a3fee03ef13e251d4d16` | Changed title only to `ci(deps): bump 12 GitHub Actions dependencies` (43 chars); original job reports 74 > 72. Sonar missing root. Workflow dependency PR remains gated, never worker-merged. |

No dependency branches force-pushed or rewritten. Compact numbered log excerpts:
[`evidence/swarm-2026-09-21/`](evidence/swarm-2026-09-21/).

## Changed files

Primary branch: `sonar-project.properties` removes only the nonexistent test
root; `scripts/verify-sonar-roots.py` checks real roots and test inventory;
`README.md` corrects release, native/fallback claims, model-test description,
commands and actual release asset/SBOM names; `docs/how-to/isolated-demo.md`
provides the blocked continuation; spec/tasks and compact evidence accompany
this report. No bridge/plugin runtime code, thresholds or exclusions changed.

Separate branch `226-repro-build`: reproducibility workflow removes the
irrelevant host cargo-clean/devShell step, retains the first Nix GC root,
keeps forced rebuild and byte comparison, and fixes upload paths to use the
Actions expression rather than a literal shell variable. Its runnable check
executes the actual comparison shell against matching, changed and missing
synthetic files. Workflow landing remains gated.

## Verification

- `python3 scripts/verify-sonar-roots.py`: red on original configuration (`tests`
  missing), green after removing only the nonexistent root. All four Rust
  integration-test files remain covered; no source/coverage exclusions added.
- `nix develop --command bash -c 'cd bridge && cargo test --all-features --locked --no-fail-fast -j 8'`:
  105 unit + 10 integration tests pass; one existing ignored doc-test. Log:
  `evidence/swarm-2026-09-21/cargo-test.txt`. Eight jobs bound this worker while
  other swarm builds share the host; no global caches cleaned.
- Latest release API: v1.0.36, published 07/06/2026; binary asset is
  `noctalia-appmenu-bridge-linux-x86_64`.

## Media outcome

**No media asset:** dimensions/duration/audio are N/A. A private Xvfb (1280×720)
→ niri → Quickshell session loaded the actual AppMenu widget from this worktree.
Bridge v1.0.36 acquired the private proxy and subscribed to private niri IPC.
Okular and Dolphin used fresh HOME/config/cache directories. The real gate is
`org.a11y.atspi.Registry` activation failure (`NameHasNoOwner: unit failed`), so
no native two-app menu/action success is claimed. The isolated QML host reports
v4.7.8-git; installed daily `noctalia` is v5.0.1, a different Rust host.

Read [the reproducible continuation](how-to/isolated-demo.md), with exact probe
source at `evidence/swarm-2026-09-21/probe-isolated.py` and runtime receipts at
`evidence/swarm-2026-09-21/isolated-plugin.txt`. No pixel capture, active-workspace
changes, desktop service restarts or private files. The probe terminated its own
process groups/display servers; no shared cache cleanup.

## PR refresh / remaining gates

- #224: REST title-only edit triggered CI run `35599186310`, **success**. Sonar
  root failure remains; workflow dependency landing requires approval.
- #222: reran failed/cancelled jobs of current-title run `35530609123`; attempt 2
  completed **success**, including Nix, title, bridge, integration and QML.
  Do not re-run the older original-title event: it replays the 73-character title.
- #219: reruns of August Sonar/coverage runs rejected HTTP 403: run created over
  a month ago. Refresh against repaired main after coordinator lands this slice;
  no empty commit/force-push to Dependabot. Historical logs returning 404 are
  explicitly unavailable, not proof of a Rust bug.
- Sonar endpoint HTTP 200 verified; the fix removes the observed indexing blocker,
  not a claim that server-side coverage/quality thresholds already pass. Existing
  first-party coverage exclusions and lack of `qualitygate.wait` are pre-existing
  debt, **not** expanded or masked here. Community branch-analysis semantics also
  need a separately approved workflow strategy; no required context removed.
- Actual Nix build + forced rebuild + `cmp` on `226-repro-build`: **exit 0**,
  both SHA-256 `f8d19779f1ab89e5ff3d804813fc5f0b81a5423e766624b66528289e1c59fedd`.
  Initial cold-dependency run hit the 360-second budget; the resumed complete
  run passed. Matching/changed/missing-byte guard check, actionlint and zizmor
  also pass. Evidence lives in #226's `docs/reproducibility-verification.md` and
  compact `docs/evidence/swarm-2026-09-21/nix-rebuild-retry.txt`.
- Opened [#225](https://github.com/yolo-labz/noctalia-appmenu/pull/225) for ordinary
  config/docs and [#226](https://github.com/yolo-labz/noctalia-appmenu/pull/226)
  for gated workflows. #226 exact head: `2c84e7c9ee8cf8ed0dadbfbc7a6dfb133f0a1b7e`.
  Both **OPEN**, CI queued/running. #226 inherits the old Sonar root until #225
  lands and coordinator updates its base. No force push necessary.
- Snapshot: `evidence/swarm-2026-09-21/pr-checks.json` records all six exact heads
  and latest-per-name check states. #222 has all returned check names success;
  #219 retains old coverage/static-analysis/Sonar failures; #223 retains
  reproducibility/Sonar; #224 retains only Sonar. A green refresh is not a
  review approval or merge claim.
- Adjacent debt logged, not bundled: `plugin/manifest.json` still advertises
  0.3.0 while the package/release is 1.0.36; the release/version pipeline needs
  its own scoped correction. Local global-hook/lefthook sync warns about an
  existing `pre-commit.old`/`pre-push.old`; commits passed configured gates after
  using the devShell's commitlint and the allowed `ci` scope, without bypass.
- Exact-head independent review and new PR CI remain coordinator gates. Primary
  docs/config slice is ordinary safe-class only once green/review-clean; workflow
  slice and #224 stay explicitly approval-gated. No worker merge/deploy.
- Reversal after any coordinator squash merge: `git revert <squash-sha>` on a
  feature branch, then PR. Nothing here is claimed merged.

